<div class="container">
  <div class="row">
    <div class="col-lg-2">
      <table class="table table-borderless">
        <thead>
          <th>ジャンル検索</th>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
          <tr>
            <td>
              <%= link_to public_items_path(name: genre.id) do %>
                <%= genre.name %>
              <% end %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>  
    </div>
    <div class="col-lg-10">
      <div class="text-center m-5">
        <span>
          ようこそ、ながのCAKEへ！<br>
          このサイトは、ケーキ販売のECサイトになります。<br>
          会員でない方も商品の閲覧はできますが、<br>
          購入には会員登録が必要になります。<br>
        </span>
      </div>
    </div>
  </div>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-lg-3">
        <%= link_to public_item_path(item.id) do %>
          <%= image_tag item.image, size: "100x100" %>
        <% end %>
        <%= item.name %>
        <%= item.price %>
      </div>
    <% end %>
    <%= link_to 'すべての商品を見る', public_items_path %>
  </div>
</div>



